# 狗头军师 Jev Chat：数据使用说明

本说明适用于公开提供的 macOS、Windows 预览包，也涵盖仓库中保留的 Android 实验源码；当前不提供 Android APK。请只分析你有权查看的会话，并在发送前核对对象与草稿。

## 读取与请求

- macOS 在用户点击读取或明确开启自动读屏后截取当前聊天区域；Apple Vision 在本机识别。选用 DeepSeek 图片识别时，该区域截图会发往用户配置的 DeepSeek 接口。
- Android 在用户启用无障碍和悬浮窗后读取所选聊天应用的可见内容；ML Kit 兜底 OCR 在本机运行。选用视觉接口时，截图会发往用户配置的视觉服务。首次安装时助手和自动分析默认关闭。
- Windows 通过窗口采集和本地 RapidOCR 获取可见聊天；当前预览版在用户开启采集且有新消息时调用配置的判断和回复服务。填入只写草稿，用户自行发送。
- 发起分析时，核对或识别出的聊天文字、必要的关系背景和提示词会发送给用户配置的 Jev 判断服务及回复模型。服务商自己的数据处理规则以其条款为准。

## 本地保存

- macOS 的两组密钥可保存在系统钥匙串；关系档案须用户明确启用，只保存有限的对象资料。设置文件不存密钥或整份聊天。
- Android 的接口密钥保存在应用私有 SharedPreferences 中，**没有额外加密**；关联上下文默认关闭，开启后会在本机保存相应聊天历史。卸载应用或清除应用数据会删除该应用的本地数据。
- Windows 的密钥保存在当前用户环境变量中，其余设置存放在程序目录的 `config.json`；运行中的聊天缓冲留在内存。可在系统用户环境变量中删除 `GOUTOU_JEV_API_KEY` 和 `GOUTOU_LLM_API_KEY`。

软件不会代替用户按发送。OCR、会话标题和说话人归属可能识别错误；请在分析与填入前核对。Windows 安装包是预览构建，尚未完成所有设备和聊天应用版本的实机验收。
